language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-backbone-browserify' ]; then cd .. && eval "mv $currentfolder generator-backbone-browserify" && cd generator-backbone-browserify; fi

