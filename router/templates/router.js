var Backbone = require('backbone');

var <%= name %> = Backbone.Router.extend({

  routes: {
    '': 'index'
  },

  index: function() {

  },

  initialize: function(options) {

  }

});
module.exports = <%= name %>;
